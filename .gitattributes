# Shell scripts and appliance config MUST stay LF — they run on Linux.
# A CRLF shebang (#!/bin/bash\r) breaks with "bad interpreter" on Ubuntu.
*.sh            text eol=lf
appliance/**    text eol=lf
Dockerfile      text eol=lf
*.yml           text eol=lf
*.yaml          text eol=lf
