# Test-only dependencies for the pytest harness (Rev 5 P0-1).
# Install alongside requirements.txt: pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==8.3.5
pytest-cov==6.0.0
